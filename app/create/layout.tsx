import "./create.css";

export const metadata = {
  title: "Reciply - Create",
  description: "Create a new recipe",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
