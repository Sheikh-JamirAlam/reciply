import "./user.css";

export const metadata = {
  title: "Reciply - Profile",
  description: "Your profile",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
